  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class='event'>
  <div class='event_details'>
    <%= form_for @event do |f| %>
    <%= f.text_field :name, :class => "field", :placeholder => "Title"%>
    <%= f.text_field :location, :class => "field", :placeholder => "Location"%>
    <%= f.date_select :date, :class => "field", :placeholder => "Date"%>
    <%= f.text_area :description, :class => "field", :placeholder => "Description"%>
    <%= f.submit %>
    <% end %>
  </div>
<div>
